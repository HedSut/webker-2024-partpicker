import { DollarToHufPipe } from './dollar-to-huf.pipe';

describe('DollarToHufPipe', () => {
  it('create an instance', () => {
    const pipe = new DollarToHufPipe();
    expect(pipe).toBeTruthy();
  });
});
